# ------------------------------------------------------------------------------
# VLLM Encode Component
#
# Encoder deployment for multimodal disaggregation scenarios (E/P/D).
# A PVC (pvc.yaml) for shared encoder embeddings cache is NOT included here —
# it is added by overlays that need it. The simulator tolerates missing PVCs.
# ------------------------------------------------------------------------------
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
